function draw(n) {
    for (let i = 1; i <= n; i++) {
      console.log('*'.repeat(n));
    }
  }
  
  draw(2);

  function draw(n) {
    for (let i = 1; i <= n; i++) {
      console.log('*'.repeat(n));
    }
  }
  
  draw(3);

  function draw(n) {
    for (let i = 1; i <= n; i++) {
      console.log('*'.repeat(n));
    }
  }
  
  draw(4);